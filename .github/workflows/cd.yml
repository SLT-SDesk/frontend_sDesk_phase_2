name: Deploy SDesk Frontend Phase 2 (Testing)

on:
  # ---------------------------------------------------------
  # Trigger on push to testing branch
  # ---------------------------------------------------------
  push:
    branches:
      - testing

  # ---------------------------------------------------------
  # Trigger workflow when a PR targets testing branch
  # ---------------------------------------------------------
  pull_request:
    branches:
      - testing

  # ---------------------------------------------------------
  # Allow manual trigger
  # ---------------------------------------------------------
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # ---------------------------------------------------------
      # STEP 1 — Checkout repository
      # ---------------------------------------------------------
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      # ---------------------------------------------------------
      # STEP 2 — Create .env file using GitHub secrets
      # ---------------------------------------------------------
      # This .env file will be uploaded to the server and placed
      # inside the frontend directory before building.
      # ---------------------------------------------------------
      - name: 🔐 Generate .env file
        run: |
          echo "VITE_APP_URL=${{ secrets.VITE_APP_URL }}" >> .env
          echo "VITE_API_BASE_URL=${{ secrets.VITE_API_BASE_URL }}" >> .env
          echo "VITE_SOCKET_URL=${{ secrets.VITE_SOCKET_URL }}" >> .env
          echo "VITE_MICROSOFT_CLIENT_ID=${{ secrets.VITE_MICROSOFT_CLIENT_ID }}" >> .env
          echo "VITE_MICROSOFT_TENANT_ID=${{ secrets.VITE_MICROSOFT_TENANT_ID }}" >> .env
          echo "AZURE_REDIRECT_URI=${{ secrets.AZURE_REDIRECT_URI }}" >> .env

      # ---------------------------------------------------------
      # STEP 3 — Upload SOURCE CODE to server
      # ---------------------------------------------------------
      # NOTE: We are NOT uploading dist/ anymore.
      # The server will run npm install + npm run build.
      #
      # Uploaded source is stored in:
      #   ~/sdesk_frontend_src_tmp/
      #
      # Later, SSH script will copy + replace real folder.
      # ---------------------------------------------------------
      - name: 📤 Upload source code to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 3251
          source: "./"
          target: "/home/${{ secrets.SSH_USERNAME }}/sdesk_frontend_src_tmp/"
          overwrite: true

      # ---------------------------------------------------------
      # STEP 4 — SSH: Clone repo (one-time) → Pull latest commits
      # Replace source with uploaded version → Build → Restart Nginx
      # ---------------------------------------------------------
      - name: 🔄 Build & Deploy on Server
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 3251
          script: |
            FRONTEND_PATH="/var/www/sdesk_new/frontend_sDesk_phase_2"   #update 6/12/2025 13.35 Fix syntax error("), 13.43 Changed from /var/www/sdesk_new/frontend_sDesk_phase_2/ to /var/www/sdesk_new/frontend_sDesk_phase_2
            # REPO_CLONE_PATH="/var/www/sdesk_new/frontend_sDesk_phase_2/"
            TMP_SRC="/home/${{ secrets.SSH_USERNAME }}/sdesk_frontend_src_tmp"

            echo "--------------------------------------------------"
            echo "Checking if the frontend repository exists..."
            echo "--------------------------------------------------"

            # -----------------------------------------------------
            # ONE-TIME CLONE STEP
            # If folder doesn't exist, we clone entire Git repo into:
            #   /var/www/sdesk_new/frontend_sDesk_phase_2
            # After that day, this block will never run again.
            # -----------------------------------------------------
            if [ ! -d "$FRONTEND_PATH" ]; then
              echo "Repository NOT found. Cloning fresh copy..."
              sudo mkdir -p /var/www/sdesk_new/frontend_sDesk_phase_2
              sudo git clone https://github.com/${{ github.repository }} $FRONTEND_PATH  #Update 6/12/2025 Changed to REPO_CLONE_PATH to FRONTEND_PATH
              sudo chown -R nginx:nginx /var/www/sdesk_new
            else
              echo "Repository exists — pulling latest changes..."
              cd $FRONTEND_PATH
              sudo git pull || true   # Avoid pipeline crash if no changes
            fi

            echo "--------------------------------------------------"
            echo "Replacing server-side source code with uploaded version..."
            echo "--------------------------------------------------"

            # Remove old files and replace with newly uploaded source code
            sudo rm -rf $FRONTEND_PATH/*
            sudo cp -r $TMP_SRC/* $FRONTEND_PATH/

            echo "--------------------------------------------------"
            echo "Copying .env file into the project..."
            echo "--------------------------------------------------"
            cp $TMP_SRC/.env $FRONTEND_PATH/.env

            echo "--------------------------------------------------"
            echo "Installing npm dependencies..."
            echo "--------------------------------------------------"
            cd $FRONTEND_PATH
            npm install

            echo "--------------------------------------------------"
            echo "Building frontend on server..."
            echo "--------------------------------------------------"
            npm run build

            echo "--------------------------------------------------"
            echo "Fixing permissions..."
            echo "--------------------------------------------------"
            sudo chown -R nginx:nginx /var/www/sdesk_new

            echo "--------------------------------------------------"
            echo "Restarting Nginx..."
            echo "--------------------------------------------------"
            sudo systemctl restart nginx

            echo "--------------------------------------------------"
            echo "Frontend Deployment Completed Successfully! 🎉"
            echo "--------------------------------------------------"
